"""
Agent 提示词模板

包含自动路由判断的提示词。
配方相关提示词位于 agent/subgraphs/recipe_gen/prompts.py
"""

# 自动路由判断提示词
AUTO_ROUTER_PROMPT = """你是一个配方领域的智能助手。请分析用户的请求，判断应该使用哪种处理模式。

## 两种模式

1. **generation（配方生成）**: 用户希望从零开始设计一个新配方
   - 特征：提供目标成分、剂型、浓度等需求
   - 示例："帮我设计一个吡唑醚菌酯 25% SC 配方"
   - 示例："我需要一个含有阿维菌素的乳油配方"

2. **optimization（配方优化）**: 用户已有一个配方，希望对其进行改进
   - 特征：提供现有配方内容，希望降本、提效、改善稳定性等
   - 示例："这个配方成本太高了，能否优化？[配方内容]"
   - 示例："帮我把这个配方中的溶剂替换掉"

## 用户请求

{user_query}

## 输出格式

请以 JSON 格式返回判断结果：

```json
{{
    "mode": "generation 或 optimization",
    "confidence": 0.0-1.0,
    "reason": "判断理由"
}}
```

注意：
- 如果用户请求中包含具体的配方内容（成分表、配比等），通常是 optimization
- 如果用户只是描述需求，没有提供现有配方，通常是 generation
- 如果无法明确判断，默认选择 generation
"""
